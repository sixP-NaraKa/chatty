generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model chatty {
  chat_id           Int    @id @map("chatId")
  chat_partner_id   Int    @map("chatPartnerId")
  chat_partner_name String @map("chatPartnerName")
}
