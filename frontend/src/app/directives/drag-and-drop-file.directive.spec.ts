import { DragAndDropFileDirective } from './drag-and-drop-file.directive';

describe('DragAndDropDirective', () => {
    it('should create an instance', () => {
        const directive = new DragAndDropFileDirective();
        expect(directive).toBeTruthy();
    });
});
